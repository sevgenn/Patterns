1) В папке "фреймворка" создал модуль amoeba/amoeba/router.py для обработки представлений.
   Запускается в модуле view как декоратор вида @Router(dict, 'path').
   В модуле main.py class Amoeba прописал метод с таким же функционалом. Он позволяет запускать подобно
   Flask или FastAPI в виде @router('/'), но только если представления написаны в модуле запуска app,
   во view не работает. Как заставить, ума не хватило.
2) Добавил @Debug (debuger.py), который пишет время в консоль.
3) Добавил вывод категорий. В шаблонах и в models/courses_models.py
4) Добавил два фреймворка в конце amoeba/amoeba/router.py. Вызов прописан в app.py.


Подсчет курсов не смог реализовать. В модели категории, кажется, правильно метод прописал, но с хранением
данных мне пока не понятно, поэтому не понимаю, откуда эти данные брать и как сохранять.