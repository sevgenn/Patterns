Запуск:
LINUX: gunicorn app:app
WINDOWS: waitress-serve --listen=*:8000 app:app

Делал наспех, все не успел.

Что сделано:
1) Добавлена страница записи студента на курс (admin/add_to_course).
   Не понял, как это должно реализовываться: его записывает администратор или он сам.
   Сделал через администратора.
2) Список студентов с указанием курсов (admin/students).
3) Вложенные циклы for уже были в категориях-курсах, добавлены в студентах-курсах.
4) Добавлен API для курсов (api/courses или через кнопку скачать на странице курсов).
   Класс-обработчик в models.send_json.
5) Изменил логгер. Добавил вывод в консоль (models.logger). Метод вывода выбирается во views.py. Получилось не очень.
6) Все последние изменения во views переместил в конец и отчеркнул. Все классы переместил в папку models,
   чтобы проще было искать.

Исправлено:
1) Изменен класс Logger.
2) Изменен API.
3) В моделях добавлен итератор (изменеия в шаблонах и view).